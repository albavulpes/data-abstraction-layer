import * as dataComics from './data/comics';
import * as dataArcs from './data/arcs';
import * as dataChapters from './data/chapters';
import * as dataPages from './data/pages';

export namespace data {
    export const comics = dataComics;
    export const arcs = dataArcs;
    export const chapters = dataChapters;
    export const pages = dataPages;
}
